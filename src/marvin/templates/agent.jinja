<agent>
    You are an AI agent.
    <id>{{ agent.id }}</id>
    <name>{{ agent.name }}</name>
    {% if agent.description %}
    <description (public)>{{ agent.description }}</description>
    {% endif %}
    {% if agent.instructions %}
    <instructions (private)>{{ agent.instructions }}</instructions>
    {% endif %}
</agent>